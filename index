<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Confirmación de Afiliación | Parque Salud</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <!-- Google Font: Sora -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --violeta-oscuro: #3D57C7;
      --lila-claro: #C2ADFF;
      --verde-claro: #ABC94A;
      --verde-oscuro: #005E5C;

      --verde-confirmar: #2E7D32;
      --rojo-rechazar: #C62828;

      --gris-fondo: #f4f6f8;
      --gris-texto: #333;
      --gris-suave: #777;
      --borde: #e0e0e0;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      background-color: var(--gris-fondo);
      font-family: 'Sora', sans-serif;
      color: var(--gris-texto);
    }

    .container {
      max-width: 860px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 12px;
      padding: 36px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.08);
    }

    h1 {
      font-size: 28px;
      font-weight: 700;
      color: var(--violeta-oscuro);
      margin-bottom: 8px;
    }

    .subtitle {
      font-size: 16px;
      font-weight: 300;
      color: #555;
      margin-bottom: 32px;
    }

    /* Steps */
    .steps {
      display: flex;
      justify-content: space-between;
      margin-bottom: 36px;
      font-size: 14px;
      font-weight: 400;
    }

    .step {
      flex: 1;
      text-align: center;
      color: #aaa;
      position: relative;
    }

    .step.active {
      color: var(--verde-oscuro);
      font-weight: 700;
    }

    /* Section */
    .section-title {
      font-size: 20px;
      font-weight: 700;
      margin-bottom: 16px;
      color: var(--verde-oscuro);
    }

    /* Cards */
    .cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 16px;
      margin-bottom: 28px;
    }

    .card {
      border: 1px solid var(--borde);
      border-radius: 10px;
      padding: 18px;
      background-color: #fafafa;
      transition: border-color 0.2s ease;
    }

    .card:hover {
      border-color: var(--lila-claro);
    }

    .card h3 {
      font-size: 15px;
      font-weight: 700;
      margin-bottom: 8px;
      color: var(--violeta-oscuro);
    }

    .card a {
      font-size: 14px;
      font-weight: 400;
      color: var(--verde-oscuro);
      text-decoration: none;
    }

    .card a:hover {
      text-decoration: underline;
    }

    /* Checkboxes */
    .checkbox-group {
      margin: 28px 0;
      font-size: 14px;
      font-weight: 400;
    }

    .checkbox-group label {
      display: flex;
      gap: 10px;
      margin-bottom: 10px;
      cursor: pointer;
    }

    /* Buttons */
    .actions {
      display: flex;
      gap: 16px;
      flex-wrap: wrap;
      margin-top: 20px;
    }

    button {
      font-family: 'Sora', sans-serif;
      font-size: 15px;
      padding: 14px 26px;
      border-radius: 8px;
      border: none;
      cursor: pointer;
      transition: transform 0.1s ease, box-shadow 0.1s ease;
    }

    button:hover {
      transform: translateY(-1px);
      box-shadow: 0 4px 8px rgba(0,0,0,0.12);
    }

    .btn-confirmar {
      background-color: var(--verde-confirmar);
      color: #ffffff;
      font-weight: 700;
    }

    .btn-rechazar {
      background-color: var(--rojo-rechazar);
      color: #ffffff;
      font-weight: 700;
    }

    .btn-secundario {
      background-color: var(--lila-claro);
      color: #2b2b2b;
      font-weight: 400;
    }

    /* Último paso */
    .last-step {
      margin-top: 48px;
      padding-top: 32px;
      border-top: 1px solid var(--borde);
    }

    .last-step p {
      font-size: 15px;
      font-weight: 300;
      margin-bottom: 20px;
    }

    .footer-note {
      margin-top: 36px;
      font-size: 13px;
      font-weight: 300;
      color: var(--gris-suave);
    }

  </style>
</head>

<body>

  <div class="container">

    <h1>¡Tu afiliación está casi lista!</h1>
    <p class="subtitle">
      Ya finalizaste la gestión comercial.  
      Solo necesitás revisar y confirmar tus datos para continuar.
    </p>

    <div class="steps">
      <div class="step">Gestión comercial</div>
      <div class="step">Carga de datos</div>
      <div class="step active">Confirmación</div>
      <div class="step">Traspaso</div>
    </div>

    <h2 class="section-title">Revisá tu información</h2>

    <div class="cards">
      <div class="card">
        <h3>🧍 Datos personales y grupo familiar</h3>
        <a href="#">Ver datos</a>
      </div>
      <div class="card">
        <h3>📄 Documentación presentada</h3>
        <a href="#">Ver documentación</a>
      </div>
      <div class="card">
        <h3>📘 Reglamento de contratación</h3>
        <a href="#">Ver reglamento</a>
      </div>
      <div class="card">
        <h3>🩺 Declaración Jurada de Salud</h3>
        <a href="#">Ver DDJJ</a>
      </div>
    </div>

    <div class="checkbox-group">
      <label>
        <input type="checkbox" /> Confirmo que los datos informados son correctos.
      </label>
      <label>
        <input type="checkbox" /> Acepto el Reglamento y la Declaración Jurada de Salud.
      </label>
    </div>

    <div class="actions">
      <button class="btn-confirmar">CONFIRMAR DATOS</button>
      <button class="btn-rechazar">RECHAZAR / SOLICITAR CORRECCIÓN</button>
    </div>

    <div class="last-step">
      <h2 class="section-title">Último paso: gestionar el traspaso</h2>
      <p>
        Una vez confirmados los datos, solo resta realizar el traspaso para activar tu cobertura.
      </p>

      <div class="actions">
        <button class="btn-secundario">Ver instructivo de traspaso</button>
        <button class="btn-secundario">Adjuntar formulario</button>
        <button class="btn-secundario">Quiero que me contacte un asesor</button>
      </div>
    </div>

    <p class="footer-note">
      Si necesitás ayuda, podés solicitar asistencia desde este portal.
    </p>

  </div>

</body>
</html>
